/**
 * Where the source lives.
 *
 * The only outbound address in the interface, written once. An anchor somebody
 * clicks is not a request this page makes, so it costs the privacy promise
 * nothing — but there should still be exactly one of it.
 */
export const REPO_URL = 'https://github.com/Visaug36/Recast';
export const ISSUES_URL = `${REPO_URL}/issues`;
