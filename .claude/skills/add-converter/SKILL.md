---
name: add-converter
description: Use when adding, removing, or changing a conversion pair in Recast's registry — "add PDF to DOCX", "support ODT", "why isn't .rtf offered for this file", a new engine module under lib/registry/converters/, a change to a pair's fidelity or caveat, or adding a format to the Format union.
---

# Adding a conversion pair

**First: check whether you need one.** Recast offers 114 pairs and declares 44
converters. A pair is a route of one or two declared **edges**, so most of what
looks like a missing pair is a missing edge somewhere else — often one edge, at
the other end of the family. `references/contract.md` has the families and their
hubs; read it before writing a thirty-ninth engine module.

When you do need an edge: three steps, three files, no component changes. The
interface reads the registry and renders whatever is there.

**1. Write the engine** at `lib/registry/converters/<from>-to-<to>.ts`, exporting
`convert(input: File): Promise<ConversionResult>`. Every heavy dependency is
named inside the function, behind `await import()`. Nothing at module scope.

**2. Declare the edge** in `lib/registry/table.ts` — `from`, `to`, `fidelity`,
and a `caveat` unless it is `exact`.

**3. Wire the engine** in `lib/registry/engines.ts`:
`'<from>><to>': () => import('./converters/<from>-to-<to>').then((m) => m.convert)`.

Steps 2 and 3 live in separate files so the **page never reaches an `import()`**
— one in a module the page imports makes the bundler emit a chunk for every
engine. They cannot drift silently: `lib/registry/index.test.ts` fails if a
declared edge has no engine, or an engine has no declared edge.

**Then read the snapshot diff.** `lib/registry/routing.test.ts` pins every pair
Recast offers. One new edge routinely adds a dozen, and that is the moment to
notice whether you wanted all of them. Do not update it without reading it.

If the format is new to Recast, `references/contract.md` has the full checklist —
the `Format` union, `FORMATS` and `FAMILY`, `MIME`, detection, `EDGE_COST`, a
fixture, and the README matrix.

## Choosing a fidelity

- **`exact`** — nothing is lost. The bytes pass through, or the round trip is
  provably lossless. Claim it only if you would stake a test on it.
- **`good`** — structure survives, presentation may shift. Headings stay
  headings; the font does not.
- **`lossy`** — something the user cares about is dropped.

**A `lossy` pair must carry a `caveat`, and a test enforces it.** Name what is
lost in the user's terms — "Tables and images are dropped" — not the library's.

Fidelity is a promise about the pair; `warnings` is a report about one file.
**Anything an engine actually discarded goes in `warnings`,** every time. Silent
loss is the worst failure Recast has.

## Tests and fixtures

`converters.test.ts` is table-driven over `converters`, so a new pair is
exercised the moment it is declared: non-empty output, right MIME type, right
extension, real magic bytes, a readable sentence on a corrupt file. **That is the
floor, not the test.** Add an assertion that the content actually survived, and
run the `probe` skill's checklist against the new pair.

Fixtures live in `test/fixtures/` and are generated by `scripts/make-fixtures.mjs`
(`pnpm fixtures`) from Recast's own writers, so a reader that only appears to work
cannot pass.

## Before you call it done

```
pnpm test && pnpm lint && pnpm typecheck && pnpm build && pnpm verify:browser
```

`verify:browser` drives the real UI in Chromium. Several bugs have reached that
script and nothing earlier.

## Reference

`references/contract.md` has the full registry contract — edges versus pairs,
the families and their hubs, every type and export, the conventions engines
follow, and a complete worked example with its engine, table entry, wiring, test
and fixture. Read it before writing an engine rather than copying the nearest
neighbour.
